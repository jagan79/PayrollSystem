@model Payroll.Application.ViewModels.EmployeeViewModel



@foreach (var employee in Model.Employees)
{
    <table>
        <tr>
            <th></th>
            <th>@Html.DisplayNameFor(model => employee.LastName)</th>
        </tr>
        <tr>
            <td></td>
            <td>@Html.DisplayFor(modelemployee => employee.LastName)</td>
        </tr>
    </table>
    @employee.LastName
}